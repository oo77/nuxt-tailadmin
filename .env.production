# =============================================================================
# ATC Nuxt Application - PRODUCTION Configuration
# =============================================================================
# Этот файл используется для production деплоя (Netlify, ahost, VPS)
# =============================================================================

# -----------------------------------------------------------------------------
# DATABASE - Aiven Cloud MySQL (Production)
# -----------------------------------------------------------------------------
DATABASE_HOST=mysql-1a388a66-atc-project2025.j.aivencloud.com
DATABASE_PORT=26996
DATABASE_USER=avnadmin
DATABASE_PASSWORD=AVNS_ljaLq4-JkhfMx9XXsaF
DATABASE_NAME=defaultdb

# SSL обязателен для Aiven Cloud
DATABASE_SSL=true

# -----------------------------------------------------------------------------
# JWT - Аутентификация (ВАЖНО: используйте уникальные секреты!)
# -----------------------------------------------------------------------------
# Сгенерируйте новые ключи командой: openssl rand -base64 64
JWT_SECRET=atc-production-jwt-secret-key-change-this-to-random-64-chars-minimum
JWT_EXPIRES_IN=7d

REFRESH_TOKEN_SECRET=atc-production-refresh-token-secret-change-this-to-random-64-chars
REFRESH_TOKEN_EXPIRES_IN=30d

# -----------------------------------------------------------------------------
# TELEGRAM BOT
# -----------------------------------------------------------------------------
TELEGRAM_BOT_TOKEN=8458161582:AAE-FBLU8F0lCH3sNJHQirNyAik88b06Ikw
TELEGRAM_WEBHOOK_SECRET=atc_secret

# Webhook URL (замените на ваш Netlify URL)
# TELEGRAM_WEBHOOK_URL=https://your-site.netlify.app/api/telegram/webhook

# -----------------------------------------------------------------------------
# APPLICATION
# -----------------------------------------------------------------------------
NODE_ENV=production

# Автоматические миграции при старте сервера
AUTO_MIGRATE=true

# -----------------------------------------------------------------------------
# NETLIFY-SPECIFIC (эти переменные добавьте в Netlify Dashboard)
# -----------------------------------------------------------------------------
# Netlify не читает .env файлы при деплое!
# Добавьте все переменные выше в:
# Netlify Dashboard -> Site settings -> Environment variables
# -----------------------------------------------------------------------------
