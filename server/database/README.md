# Управление базой данных

## Доступные команды

### 📊 Статус миграций

```bash
npm run db:status
```

Показывает текущее состояние миграций: какие применены, какие ожидают применения.

---

### ⬆️ Применение миграций

```bash
npm run db:migrate
```

Применяет все непримененные миграции к базе данных.

**Когда использовать:**

- После добавления новых миграций
- При первой настройке проекта
- После обновления кода из репозитория

---

### ⬇️ Откат последней миграции

```bash
npm run db:rollback
```

Откатывает последнюю примененную миграцию.

**Когда использовать:**

- Если последняя миграция содержит ошибку
- Для тестирования миграций

---

### 🔄 Откат всех миграций

```bash
npm run db:rollback:all
```

Откатывает ВСЕ миграции, полностью очищая базу данных.

**⚠️ ВНИМАНИЕ:** Это удалит все таблицы и данные!

**Когда использовать:**

- Перед полным сбросом БД
- При критических изменениях структуры

---

### 🌱 Создание начальных данных

```bash
npm run db:seed
```

Создаёт администратора по умолчанию (если пользователей ещё нет).

**Создаваемые данные:**

- Username: `admin`
- Email: `admin@example.com`
- Password: `admin123`
- Role: `admin`

**⚠️ ВАЖНО:** Смените пароль после первого входа!

---

### 🔥 Полный сброс БД

```bash
npm run db:reset
```

**Комплексная команда**, которая выполняет:

1. Откат всех миграций (`db:rollback:all`)
2. Применение всех миграций заново (`db:migrate`)
3. Создание администратора (`db:seed`)

**⚠️ ВНИМАНИЕ:** Это полностью очистит и пересоздаст БД!

**Когда использовать:**

- При первой настройке проекта
- Для возврата к чистому состоянию
- При критических проблемах с БД
- В процессе разработки для тестирования

**После выполнения вы получите:**

- Чистую БД с актуальной структурой
- Администратора с дефолтными учётными данными

---

## Типичные сценарии использования

### 🆕 Первая настройка проекта

```bash
npm run db:reset
```

### 📝 Добавление новой миграции

```bash
npm run db:migrate
```

### 🐛 Откат ошибочной миграции

```bash
npm run db:rollback
# Исправить миграцию
npm run db:migrate
```

### 🔄 Полный сброс для тестирования

```bash
npm run db:reset
```

### 📊 Проверка состояния

```bash
npm run db:status
```

---

## Создание новой миграции

1. Создайте файл в `server/database/migrations/`:

   ```
   YYYYMMDD_NNN_description.ts
   ```

   Пример: `20260105_029_add_user_settings.ts`

2. Используйте шаблон из `_template.ts`

3. Зарегистрируйте миграцию в `server/database/migrator.ts`:

   ```typescript
   import * as yourMigration from "./migrations/YYYYMMDD_NNN_description";

   const MIGRATIONS_REGISTRY: Migration[] = [
     // ... существующие миграции
     {
       name: "YYYYMMDD_NNN_description",
       up: yourMigration.up,
       down: yourMigration.down,
       description: yourMigration.description,
     },
   ];
   ```

4. Примените миграцию:
   ```bash
   npm run db:migrate
   ```

---

## Безопасность

### ⚠️ Продакшн

На продакшене **НИКОГДА** не используйте:

- `db:reset`
- `db:rollback:all`

Используйте только:

- `db:migrate` — для применения новых миграций
- `db:status` — для проверки состояния

### 🔒 Смена пароля администратора

После первого входа с дефолтными учётными данными **обязательно** смените пароль через интерфейс системы.

---

## Устранение проблем

### Миграции не применяются

```bash
npm run db:status  # Проверить состояние
npm run db:migrate # Попробовать снова
```

### Ошибка в миграции

```bash
npm run db:rollback  # Откатить
# Исправить код миграции
npm run db:migrate   # Применить снова
```

### БД в неконсистентном состоянии

```bash
npm run db:reset  # Полный сброс (только для разработки!)
```
