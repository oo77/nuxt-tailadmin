/**
 * Plugin для инициализации аутентификации при загрузке приложения
 * 
 * Автоматически проверяет наличие токена и верифицирует его
 * Восстанавливает сессию пользователя если токен валиден
 */
export default defineNuxtPlugin(async () => {
  const { init } = useAuth()
  
  // Инициализируем auth при загрузке приложения
  await init()
})
