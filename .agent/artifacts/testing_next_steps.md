# Что делать дальше

## ✅ Внесённые изменения

1. **Исправлена проблема с множественными тестами**
   - Теперь можно назначить несколько разных тестов на одно занятие
   - Проверка дублирования работает корректно

2. **Добавлено детальное логирование**
   - Серверные логи: `[API tests/assignments]`
   - Клиентские логи: `[Schedule]`

## 🔄 Следующие шаги

### 1. Перезапустите dev-сервер

```bash
# Остановите текущий сервер (Ctrl+C)
# Затем запустите снова:
npm run dev
```

### 2. Попробуйте создать занятие "Проверка знаний"

1. Откройте `/schedule`
2. Создайте новое занятие:
   - Выберите группу
   - Выберите дисциплину (к которой привязан тест)
   - Тип: **Проверка знаний**
   - Заполните дату и время
3. Нажмите "Добавить"

### 3. Проверьте логи

**В консоли браузера (F12 → Console):**
```
[Schedule] Создание назначения теста: ...
[Schedule] Ответ сервера: { ... }
```

**В терминале сервера:**
```
[API tests/assignments] Получен запрос: ...
[API tests/assignments] Проверка шаблона теста: ...
[API tests/assignments] Создание назначения...
```

### 4. Если ошибка повторяется

Скопируйте и отправьте мне:
- ✅ Полный вывод из консоли браузера
- ✅ Полный вывод из терминала сервера
- ✅ Скриншот ошибки (если есть)

## 🔍 Проверка данных в БД

Если хотите проверить данные напрямую, выполните:

```sql
-- Проверяем привязку тестов к дисциплинам
SELECT 
    dt.id,
    d.name as discipline_name,
    tt.name as template_name,
    dt.is_required
FROM discipline_tests dt
LEFT JOIN disciplines d ON dt.discipline_id = d.id
LEFT JOIN test_templates tt ON dt.test_template_id = tt.id;
```

Если результат пустой — значит тесты не привязаны к дисциплинам!

## 📋 Быстрая проверка

Чтобы убедиться, что всё настроено:

1. **Есть ли банк вопросов?** → `/test-bank`
2. **Есть ли шаблон теста?** → `/test-bank/templates`
3. **Привязан ли тест к дисциплине?** → `/programs` → выберите курс → дисциплина → раздел "Тесты"
4. **Есть ли студенты в группе?** → `/groups/[id]` → вкладка "Студенты"

Если хотя бы один пункт не выполнен — тесты не будут работать!
