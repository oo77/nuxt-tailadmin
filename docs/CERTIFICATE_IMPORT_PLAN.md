# 📜 ПЛАН РЕАЛИЗАЦИИ: ИМПОРТ СЕРТИФИКАТОВ ИЗ EXCEL

> **Дата создания:** 2026-01-06  
> **Статус:** 📋 Планирование  
> **Приоритет:** Высокий

---

## 📋 ОГЛАВЛЕНИЕ

1. [Обзор задачи](#обзор-задачи)
2. [Анализ текущей системы](#анализ-текущей-системы)
3. [Новая архитектура](#новая-архитектура)
4. [Пошаговый процесс импорта](#пошаговый-процесс-импорта)
5. [Дорожная карта реализации](#дорожная-карта-реализации)
6. [Технические детали](#технические-детали)
7. [Чеклист готовности](#чеклист-готовности)

---

## 🎯 ОБЗОР ЗАДАЧИ

### Проблема

Ранее сертификаты велись в Excel. Необходимо реализовать массовый импорт исторических данных о выданных сертификатах в систему.

### Решение

Создать многошаговый мастер импорта сертификатов из Excel-файла с:
- Выбором курса (существующего или ручным вводом названия)
- Загрузкой Excel-файла
- Настройкой шаблона URL для PDF-файлов
- Анализом и предпросмотром данных
- Массовым импортом в базу данных

### Формат входных данных (Excel)

| Колонка | Описание | Формат | Обязательность |
|---------|----------|--------|----------------|
| ПИНФЛ | Уникальный идентификатор | 14 цифр | ✅ Обязательно |
| ФИО | Полное имя слушателя | Текст | ✅ Обязательно |
| Организация | Название организации | Текст | ✅ Обязательно |
| Должность | Должность слушателя | Текст | ✅ Обязательно |
| Серия/Номер | Номер сертификата | Текст | ✅ Обязательно |
| Дата выдачи | Дата выдачи сертификата | **DD.MM.YYYY** (например: 15.03.2025) | ✅ Обязательно |

**Пример Excel-файла:**

| ПИНФЛ | ФИО | Организация | Должность | Серия/Номер | Дата выдачи |
|-------|-----|-------------|-----------|-------------|-------------|
| 12345678901234 | Иванов Иван Иванович | ООО "Авиакомпания" | Инженер | ATC25-001 | 15.03.2025 |
| 98765432109876 | Петрова Мария Сергеевна | АО "Аэропорт" | Менеджер | ATC25-002 | 20.03.2025 |

### Формат URL сертификата

```
http://edu.uzairports.com/certificates/ATC25_{НОМЕР}{ФИО}.pdf
```

---

## 🔍 АНАЛИЗ ТЕКУЩЕЙ СИСТЕМЫ

### Текущая структура таблицы `issued_certificates`

```sql
issued_certificates
├── id (UUID, PK)
├── group_id (UUID, FK → study_groups) — ОБЯЗАТЕЛЬНЫЙ ❌
├── student_id (UUID, FK → students) — ОБЯЗАТЕЛЬНЫЙ ✅
├── template_id (UUID, FK → certificate_templates) — ОБЯЗАТЕЛЬНЫЙ ❌
├── certificate_number (VARCHAR)
├── issue_date (DATE)
├── expiry_date (DATE, NULL)
├── pdf_file_url (VARCHAR, NULL)
├── docx_file_url (VARCHAR, NULL)
├── status (ENUM: draft, issued, revoked)
├── variables_data (JSON, NULL)
├── warnings (JSON, NULL)
├── legacy_id (VARCHAR, NULL)
└── ... timestamps
```

### Проблема текущей архитектуры

- `group_id` и `template_id` обязательны
- При импорте legacy-сертификатов приходится создавать фиктивные группы
- Это создаёт "мусор" в базе данных

### Два сценария выдачи сертификата

| Сценарий | Связь с группой | Источник данных |
|----------|-----------------|-----------------|
| **Через журнал группы** | ✅ Есть `group_id` | Данные из связей (курс, шаблон) |
| **Ручной ввод / Импорт** | ❌ Нет `group_id` | Данные в текстовых полях записи |

---

## 🏗️ НОВАЯ АРХИТЕКТУРА

### Изменения в таблице `issued_certificates`

```sql
-- Миграция: make group_id and template_id optional, add standalone fields

ALTER TABLE issued_certificates
  -- Делаем опциональными
  MODIFY COLUMN group_id VARCHAR(191) NULL,
  MODIFY COLUMN template_id VARCHAR(191) NULL;

ALTER TABLE issued_certificates
  -- Standalone данные о курсе
  ADD COLUMN course_name VARCHAR(255) NULL 
    COMMENT 'Название курса (для standalone сертификатов)',
  ADD COLUMN course_code VARCHAR(50) NULL 
    COMMENT 'Код курса',
  ADD COLUMN course_hours INT NULL 
    COMMENT 'Количество часов курса',
  
  -- Standalone данные о группе (опционально)
  ADD COLUMN group_code VARCHAR(50) NULL 
    COMMENT 'Код группы (текст)',
  ADD COLUMN group_start_date DATE NULL 
    COMMENT 'Дата начала обучения',
  ADD COLUMN group_end_date DATE NULL 
    COMMENT 'Дата окончания обучения',
  
  -- Источник создания
  ADD COLUMN source_type ENUM('group_journal', 'manual', 'import') 
    NOT NULL DEFAULT 'group_journal'
    COMMENT 'Источник создания сертификата';
```

### Логика заполнения полей

```
┌─────────────────────────────────────────────────────────────────┐
│                    СЕРТИФИКАТ                                   │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  IF source_type = 'group_journal':                              │
│    ├── group_id → study_groups → courses (данные из связей)     │
│    ├── template_id → certificate_templates                      │
│    └── course_name, course_code, etc. = NULL                    │
│                                                                  │
│  IF source_type = 'manual' | 'import':                          │
│    ├── group_id = NULL                                          │
│    ├── template_id = NULL                                        │
│    ├── course_name = "Курс повышения квалификации"              │
│    ├── course_code = "ATC25"                                    │
│    └── все данные хранятся в самой записи                       │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 📊 ПОШАГОВЫЙ ПРОЦЕСС ИМПОРТА

### ШАГ 1: Выбор курса и срок действия

```
┌─────────────────────────────────────────────────────────────────┐
│  ШАГ 1: НАСТРОЙКА КУРСА                                    1/5  │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  📚 ДЛЯ КАКОГО КУРСА ИМПОРТИРУЮТСЯ СЕРТИФИКАТЫ?                 │
│                                                                  │
│  ○ Выбрать существующий курс                                    │
│    ┌─────────────────────────────────────────────────────────┐  │
│    │ ▼ Выберите курс из списка                               │  │
│    ├─────────────────────────────────────────────────────────┤  │
│    │   Курс повышения квалификации АТС-001 (72 часа)         │  │
│    │   Базовый курс ATPL-002 (240 часов)                     │  │
│    │   Курс переподготовки RAMP-003 (40 часов)               │  │
│    └─────────────────────────────────────────────────────────┘  │
│                                                                  │
│  ● Указать название курса вручную                               │
│    ┌─────────────────────────────────────────────────────────┐  │
│    │ Название курса *                                        │  │
│    │ [Курс повышения квалификации                         ]  │  │
│    │                                                          │  │
│    │ Код курса (опционально)                                 │  │
│    │ [ATC25                                               ]  │  │
│    │                                                          │  │
│    │ Количество часов (опционально)                          │  │
│    │ [72                                                  ]  │  │
│    └─────────────────────────────────────────────────────────┘  │
│                                                                  │
│  ─────────────────────────────────────────────────────────────  │
│                                                                  │
│  📅 СРОК ДЕЙСТВИЯ СЕРТИФИКАТОВ                                  │
│  (применяется ко всем импортируемым сертификатам)               │
│                                                                  │
│  ○ Бессрочный (сертификаты не истекают)                         │
│                                                                  │
│  ● Указать срок действия                                        │
│    ┌─────────────────────────────────────────────────────────┐  │
│    │ Срок действия: [ 12 ▼] месяцев от даты выдачи          │  │
│    │                                                          │  │
│    │ ℹ️ Дата истечения будет рассчитана автоматически        │  │
│    │    для каждого сертификата:                             │  │
│    │    expiry_date = issue_date + 12 месяцев                │  │
│    └─────────────────────────────────────────────────────────┘  │
│                                                                  │
│                                              [Далее →]          │
└─────────────────────────────────────────────────────────────────┘
```

**Логика:**
- Если выбран существующий курс → берём `course_name`, `course_code`, `course_hours` из БД
- Если ручной ввод → пользователь вводит данные
- Срок действия применяется **ко всем** сертификатам в импорте
- `expiry_date` рассчитывается: `issue_date` + N месяцев

---

### ШАГ 2: Загрузка Excel-файла

```
┌─────────────────────────────────────────────────────────────────┐
│  ШАГ 2: ЗАГРУЗКА ФАЙЛА                                     2/5  │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ℹ️ Требования к файлу:                                         │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │ • Формат: .xlsx или .xls                                    ││
│  │ • Первая строка — заголовки                                 ││
│  │ • Обязательные колонки:                                     ││
│  │   ПИНФЛ | ФИО | Организация | Должность | Номер | Дата     ││
│  └─────────────────────────────────────────────────────────────┘│
│                                                                  │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │                                                              ││
│  │              📁 Перетащите файл сюда                        ││
│  │                      или                                    ││
│  │               [Выбрать файл]                                ││
│  │                                                              ││
│  │              Поддерживаемые форматы: .xlsx, .xls            ││
│  │                                                              ││
│  └─────────────────────────────────────────────────────────────┘│
│                                                                  │
│  📄 Пример структуры файла:                                     │
│  ┌──────────────┬─────────────────┬──────────┬────────┬────────┐│
│  │ ПИНФЛ        │ ФИО             │ Орг-ция  │ Должн. │ Номер  ││
│  ├──────────────┼─────────────────┼──────────┼────────┼────────┤│
│  │12345678901234│Иванов Иван Иван.│ООО "X"   │Инженер │ATC25-01││
│  │98765432109876│Петрова Мария С. │АО "Y"    │Менеджер│ATC25-02││
│  └──────────────┴─────────────────┴──────────┴────────┴────────┘│
│                                                                  │
│                                  [← Назад]     [Далее →]        │
└─────────────────────────────────────────────────────────────────┘
```

---

### ШАГ 3: Настройка URL и опций

```
┌─────────────────────────────────────────────────────────────────┐
│  ШАГ 3: НАСТРОЙКА ИМПОРТА                                  3/5  │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  🔗 Шаблон ссылки на PDF-файл сертификата:                      │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │http://edu.uzairports.com/certificates/ATC25_{NUM}{FIO}.pdf  ││
│  └─────────────────────────────────────────────────────────────┘│
│                                                                  │
│  Доступные переменные:                                          │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │ {NUM}    — Серийный номер сертификата (из Excel)            ││
│  │ {FIO}    — ФИО слушателя (подчёркивания вместо пробелов)    ││
│  │ {PINFL}  — ПИНФЛ слушателя                                  ││
│  │ {DATE}   — Дата выдачи (YYYYMMDD)                           ││
│  └─────────────────────────────────────────────────────────────┘│
│                                                                  │
│  📝 Пример результата:                                          │
│  http://edu.uzairports.com/certificates/ATC25_ATC25-001Иванов..│
│                                                                  │
│  ─────────────────────────────────────────────────────────────  │
│                                                                  │
│  ⚙️ Дополнительные опции:                                       │
│                                                                  │
│  ☑ Создавать новых слушателей, если ПИНФЛ не найден            │
│    └── Слушатель будет создан с данными из Excel               │
│                                                                  │
│  ☐ Обновлять существующие сертификаты                          │
│    └── Если номер сертификата уже есть — обновить данные       │
│                                                                  │
│  ☐ Пропускать строки с ошибками                                │
│    └── Продолжить импорт даже при ошибках в некоторых строках  │
│                                                                  │
│                                  [← Назад]     [Анализировать]  │
└─────────────────────────────────────────────────────────────────┘
```

---

### ШАГ 4: Анализ и предпросмотр

```
┌─────────────────────────────────────────────────────────────────┐
│  ШАГ 4: АНАЛИЗ ДАННЫХ                                      4/5  │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  📊 Сводка анализа                                              │
│  ┌─────────────┬─────────────┬─────────────┬─────────────┐     │
│  │   Всего     │  Валидных   │   Ошибок    │ Дубликатов  │     │
│  │    150      │    145      │      3      │      2      │     │
│  │   строк     │   записей   │   строк     │   записей   │     │
│  │             │  (готово)   │ (пропуск)   │ (пропуск)   │     │
│  └─────────────┴─────────────┴─────────────┴─────────────┘     │
│                                                                  │
│  ┌─────────────┬─────────────┐                                  │
│  │ Новых       │ Существующих│                                  │
│  │ слушателей  │ слушателей  │                                  │
│  │    12       │    133      │                                  │
│  │  (создать)  │  (найдены)  │                                  │
│  └─────────────┴─────────────┘                                  │
│                                                                  │
│  ⚠️ Ошибки валидации (3 строки):                                │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │ Строка 45: Неверный формат ПИНФЛ (12 цифр вместо 14)        ││
│  │ Строка 78: Пустое поле "Номер сертификата"                  ││
│  │ Строка 102: Неверный формат даты                            ││
│  └─────────────────────────────────────────────────────────────┘│
│                                                                  │
│  📋 Предпросмотр данных (первые 20 записей):                    │
│  ┌────┬──────────────┬─────────────────┬──────────┬────────────┐│
│  │ №  │ ПИНФЛ        │ ФИО             │ Номер    │ Статус     ││
│  ├────┼──────────────┼─────────────────┼──────────┼────────────┤│
│  │ 1  │12345678901234│Иванов Иван И.   │ATC25-001 │ ✓ Готов    ││
│  │ 2  │98765432109876│Петрова Мария С. │ATC25-002 │ ⚠ Новый    ││
│  │ 3  │11111111111111│Сидоров Петр А.  │ATC25-003 │ ✓ Готов    ││
│  │...│              │                 │          │            ││
│  └────┴──────────────┴─────────────────┴──────────┴────────────┘│
│                                                                  │
│                       [← Назад]     [Импортировать 145 записей] │
└─────────────────────────────────────────────────────────────────┘
```

---

### ШАГ 5: Прогресс и результаты

```
┌─────────────────────────────────────────────────────────────────┐
│  ШАГ 5: ИМПОРТ                                             5/5  │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ⏳ Выполняется импорт сертификатов...                          │
│                                                                  │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │ ████████████████████████████████░░░░░░░░░░  78%             ││
│  │                                                              ││
│  │ Обработано: 113 из 145                                      ││
│  │ Создано слушателей: 10 из 12                                ││
│  │ Импортировано сертификатов: 103                             ││
│  └─────────────────────────────────────────────────────────────┘│
│                                                                  │
│  📝 Текущая операция:                                           │
│  Импорт сертификата ATC25-114 для Козлов Андрей Викторович...  │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘

                              ↓ После завершения ↓

┌─────────────────────────────────────────────────────────────────┐
│  ✅ ИМПОРТ ЗАВЕРШЁН                                             │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  📊 Результаты импорта                                          │
│  ┌─────────────┬─────────────┬─────────────┬─────────────┐     │
│  │  Успешно    │  Пропущено  │   Ошибок    │   Время     │     │
│  │    145      │      3      │      2      │   00:23     │     │
│  │ сертификат. │   строк     │   записей   │   секунды   │     │
│  └─────────────┴─────────────┴─────────────┴─────────────┘     │
│                                                                  │
│  ✓ Создано новых слушателей: 12                                 │
│  ✓ Импортировано сертификатов: 145                              │
│  ✓ Курс: Курс повышения квалификации (ATC25)                   │
│                                                                  │
│  ⚠️ Пропущенные записи:                                         │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │ Строка 45: Неверный ПИНФЛ                                   ││
│  │ Строка 78: Пустой номер сертификата                         ││
│  │ Строка 102: Неверная дата                                   ││
│  └─────────────────────────────────────────────────────────────┘│
│                                                                  │
│           [Новый импорт]     [Перейти к базе сертификатов]     │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🗺️ ДОРОЖНАЯ КАРТА РЕАЛИЗАЦИИ

### ЭТАП 1: Миграция базы данных
**Приоритет:** 🔴 Критический  
**Статус:** ✅ Завершён

**Задачи:**
- [x] Создать миграцию `20260106_034_certificate_standalone.ts`
- [x] Сделать `group_id` и `template_id` опциональными
- [x] Добавить поля: `course_name`, `course_code`, `course_hours`
- [x] Добавить поля: `group_code`, `group_start_date`, `group_end_date`
- [x] Добавить поле: `source_type` (enum: group_journal, manual, import)
- [x] Мигрировать существующие данные
- [x] Протестировать миграцию

**Файлы:**
```
server/database/migrations/20260106_034_certificate_standalone.ts
```

---

### ЭТАП 2: Обновление типов TypeScript
**Приоритет:** 🔴 Критический  
**Статус:** ✅ Завершён

**Задачи:**
- [x] Обновить `IssuedCertificate` в `server/types/certificate.ts`
- [x] Добавить `CertificateSourceType`
- [x] Обновить `app/types/certificate.ts`
- [x] Создать типы для импорта в `types/import.ts`

**Файлы:**
```
server/types/certificate.ts
app/types/certificate.ts
types/import.ts (расширить)
```

---

### ЭТАП 3: Обновление репозитория
**Приоритет:** 🔴 Критический  
**Статус:** ✅ Завершён

**Задачи:**
- [x] Обновить `mapRowToIssuedCertificate` в репозитории
- [x] Обновить `createIssuedCertificate` — добавить source_type
- [x] Добавить функцию `createStandaloneCertificate`
- [x] Добавить функцию `getCertificateByNumber`

**Файлы:**
```
server/repositories/certificateTemplateRepository.ts
```

---

### ЭТАП 4: API импорта сертификатов
**Приоритет:** 🟠 Высокий  
**Статус:** ✅ Завершён

**Задачи:**
- [x] Создать `POST /api/certificates/import/analyze`
  - Парсинг Excel (xlsx)
  - Валидация строк
  - Проверка слушателей по ПИНФЛ
  - Генерация URL по шаблону
  - Возврат анализа
- [x] Создать `POST /api/certificates/import/execute`
  - Создание слушателей (если опция включена)
  - Массовое создание сертификатов
  - Возврат jobId для отслеживания
- [x] Создать `GET /api/certificates/import/status/[id]`
  - Получение статуса задачи импорта
- [x] Создать утилиты импорта сертификатов

**Файлы:**
```
server/api/certificates/import/analyze.post.ts
server/api/certificates/import/execute.post.ts
server/api/certificates/import/status/[id].get.ts
server/utils/certificateImportUtils.ts
app/types/import.ts (расширен типами для сертификатов)
```

---

### ЭТАП 5: Компоненты UI импорта
**Приоритет:** 🟠 Высокий  
**Статус:** ✅ Завершён

**Задачи:**
- [x] Создать `CertificateImportCourseSelect.vue` — выбор курса и срока действия (шаг 1)
- [x] Создать `CertificateImportUploader.vue` — загрузка файла (шаг 2)
- [x] Создать `CertificateImportSettings.vue` — настройки URL и опции (шаг 3)
- [x] Создать `CertificateImportAnalysis.vue` — анализ и предпросмотр (шаг 4)
- [x] Создать `CertificateImportProgress.vue` — прогресс импорта (шаг 5)
- [x] Создать `CertificateImportResults.vue` — результаты импорта (шаг 5)

**Файлы:**
```
app/components/database/CertificateImportCourseSelect.vue
app/components/database/CertificateImportUploader.vue
app/components/database/CertificateImportSettings.vue
app/components/database/CertificateImportAnalysis.vue
app/components/database/CertificateImportProgress.vue
app/components/database/CertificateImportResults.vue
```

---

### ЭТАП 6: Страница импорта
**Приоритет:** 🟠 Высокий  
**Статус:** ✅ Завершён

**Задачи:**
- [x] Создать страницу `app/pages/database/import-certificates.vue`
- [x] Реализовать пошаговую навигацию (5 шагов)
- [x] Интегрировать компоненты
- [x] Добавить обработку ошибок и уведомления

**Файлы:**
```
app/pages/database/import-certificates.vue
```

---

### ЭТАП 7: Интеграция в интерфейс
**Приоритет:** 🟡 Средний  
**Статус:** ✅ Завершён

**Задачи:**
- [x] Добавить кнопку "Импорт из Excel" в `CertificateManagementPanel.vue`
- [x] Обновить отображение сертификатов с учётом новых полей
- [x] Добавить фильтр по `source_type`
- [x] Обновить детальный просмотр сертификата

**Файлы:**
```
app/components/database/CertificateManagementPanel.vue
app/components/database/CertificateDetailModal.vue
server/api/certificates/index.get.ts
```

---

### ЭТАП 8: Ручное добавление сертификата
**Приоритет:** 🟢 Низкий  
**Статус:** ✅ Завершён

**Задачи:**
- [x] Создать новый компонент `CertificateManualFormModal.vue` для standalone сертификатов
- [x] Добавить возможность выбора существующего слушателя или создания нового
- [x] Добавить возможность выбора существующего курса или ручного ввода
- [x] Реализовать API endpoint `POST /api/certificates/manual`
- [x] Интегрировать кнопку "Добавить вручную" в `CertificateManagementPanel.vue`

**Файлы:**
```
app/components/database/CertificateManualFormModal.vue
server/api/certificates/manual.post.ts
app/components/database/CertificateManagementPanel.vue (обновлён)
```

---

## 🔧 ТЕХНИЧЕСКИЕ ДЕТАЛИ

### Структура типов для импорта

```typescript
// types/import.ts

export interface CertificateImportConfig {
  // Источник курса
  courseSource: 'existing' | 'manual';
  courseId?: string;           // Если выбран существующий курс
  courseName?: string;         // Если ручной ввод
  courseCode?: string;
  courseHours?: number;
  
  // Срок действия сертификатов
  validityType: 'unlimited' | 'months';  // Бессрочный или с ограничением
  validityMonths?: number;               // Количество месяцев (если validityType = 'months')
  // expiry_date рассчитывается: issue_date + validityMonths
  
  // Настройки URL
  urlTemplate: string;         // Шаблон URL с переменными
  
  // Опции импорта
  createStudents: boolean;     // Создавать ли новых слушателей
  updateExisting: boolean;     // Обновлять ли существующие сертификаты
  skipErrors: boolean;         // Пропускать ли строки с ошибками
}

export interface CertificateImportRow {
  rowNumber: number;
  pinfl: string;
  fullName: string;
  organization: string;
  position: string;
  certificateNumber: string;
  issueDate: string;
  generatedUrl: string;
  
  // Статус проверки
  status: 'valid' | 'warning' | 'error';
  studentStatus: 'exists' | 'new' | 'not_found';
  certificateStatus: 'new' | 'duplicate';
  errors: string[];
  warnings: string[];
}

export interface CertificateImportAnalysis {
  // Статистика
  totalRows: number;
  validRows: number;
  errorRows: number;
  duplicateRows: number;
  
  // Слушатели
  existingStudents: number;
  newStudents: number;
  
  // Сертификаты
  newCertificates: number;
  duplicateCertificates: number;
  
  // Данные
  preview: CertificateImportRow[];
  errors: { rowNumber: number; errors: string[] }[];
  
  // Ссылки на существующих
  existingPinfls: string[];
  existingCertNumbers: string[];
}

export interface CertificateImportProgress {
  status: 'pending' | 'processing' | 'completed' | 'failed';
  totalRecords: number;
  processedRecords: number;
  createdStudents: number;
  createdCertificates: number;
  errors: { rowNumber: number; error: string }[];
  startedAt: Date;
  completedAt?: Date;
}
```

### Валидация Excel-строки

```typescript
function validateRow(row: any, rowNumber: number): CertificateImportRow {
  const errors: string[] = [];
  const warnings: string[] = [];
  
  // ПИНФЛ: ровно 14 цифр
  const pinfl = String(row.pinfl || row['ПИНФЛ'] || '').trim();
  if (!pinfl) {
    errors.push('ПИНФЛ не указан');
  } else if (!/^\d{14}$/.test(pinfl)) {
    errors.push(`ПИНФЛ должен содержать 14 цифр (найдено: ${pinfl.length})`);
  }
  
  // ФИО: не пустое
  const fullName = String(row.fullName || row['ФИО'] || '').trim();
  if (!fullName) {
    errors.push('ФИО не указано');
  }
  
  // Номер сертификата: не пустой
  const certNumber = String(row.certificateNumber || row['Номер'] || row['Серия'] || '').trim();
  if (!certNumber) {
    errors.push('Номер сертификата не указан');
  }
  
  // Дата выдачи: валидный формат
  const issueDate = parseDate(row.issueDate || row['Дата'] || row['Дата выдачи']);
  if (!issueDate) {
    errors.push('Неверный формат даты выдачи');
  }
  
  return {
    rowNumber,
    pinfl,
    fullName,
    organization: String(row.organization || row['Организация'] || '').trim(),
    position: String(row.position || row['Должность'] || '').trim(),
    certificateNumber: certNumber,
    issueDate: issueDate?.toISOString() || '',
    generatedUrl: '',
    status: errors.length > 0 ? 'error' : 'valid',
    studentStatus: 'not_found', // будет обновлено после проверки БД
    certificateStatus: 'new',   // будет обновлено после проверки БД
    errors,
    warnings,
  };
}
```

### Генерация URL по шаблону

```typescript
function generateCertificateUrl(
  template: string,
  row: CertificateImportRow
): string {
  // Замена переменных
  return template
    .replace(/{NUM}/g, row.certificateNumber)
    .replace(/{FIO}/g, row.fullName.replace(/\s+/g, '_'))
    .replace(/{PINFL}/g, row.pinfl)
    .replace(/{DATE}/g, row.issueDate.replace(/-/g, ''));
}

// Пример:
// template: "http://edu.uzairports.com/certificates/ATC25_{NUM}{FIO}.pdf"
// row: { certificateNumber: "001", fullName: "Иванов Иван Иванович" }
// result: "http://edu.uzairports.com/certificates/ATC25_001Иванов_Иван_Иванович.pdf"
```

---

## ✅ ЧЕКЛИСТ ГОТОВНОСТИ

### Перед началом разработки
- [ ] Согласовать формат URL (как формируется `{FIO}`)
- [ ] Подтвердить автосоздание слушателей
- [ ] Определить поведение при дубликатах

### После разработки
- [ ] Миграция БД выполнена успешно
- [ ] Все API endpoint'ы работают
- [ ] UI компоненты отображаются корректно
- [ ] Валидация Excel работает
- [ ] Импорт выполняется без ошибок
- [ ] Сертификаты отображаются в базе
- [ ] Кнопка импорта добавлена в панель управления

### Тестирование
- [ ] Тест: импорт 10 записей
- [ ] Тест: импорт 1000+ записей
- [ ] Тест: обработка ошибок
- [ ] Тест: создание новых слушателей
- [ ] Тест: обнаружение дубликатов

---

## 📝 ПРИМЕЧАНИЯ

### Открытые вопросы

1. **Формат FIO в URL:**
   - Текущее предположение: `Иванов_Иван_Иванович` (подчёркивания)
   - Требуется подтверждение

2. **Создание слушателей:**
   - Текущее предположение: опционально (чекбокс)
   - Данные берутся из Excel (ФИО, организация, должность)

3. **Дубликаты сертификатов:**
   - Текущее предположение: пропускать с предупреждением
   - Опционально: обновлять

---

## 📅 ИСТОРИЯ ИЗМЕНЕНИЙ

| Дата | Версия | Описание |
|------|--------|----------|
| 2026-01-06 | 1.0 | Создание плана |
| 2026-01-06 | 1.1 | Добавлен срок действия сертификатов на Шаг 1; Уточнён формат даты (DD.MM.YYYY) |
| 2026-01-06 | 1.2 | ✅ Этап 1-3: Миграция БД, Типы TypeScript, Репозиторий |
| 2026-01-06 | 1.3 | ✅ Этап 4: API импорта сертификатов (analyze, execute, status) |
| 2026-01-06 | 1.4 | ✅ Этап 5: UI компоненты импорта (6 компонентов) |
| 2026-01-06 | 1.5 | ✅ Этап 6: Страница импорта сертификатов |
| 2026-01-06 | 1.6 | ✅ Этап 7: Интеграция в интерфейс (кнопка импорта, фильтр source_type, обновление отображения) |
| 2026-01-06 | 1.7 | ✅ Этап 8: Ручное добавление сертификата (API, форма, интеграция) |

---

## 📖 ИСПОЛЬЗОВАНИЕ API ИМПОРТА

### Шаг 1: Анализ файла

```bash
# POST /api/certificates/import/analyze
# Content-Type: multipart/form-data

# Параметры:
# - file: Excel файл (.xlsx, .xls)
# - config: JSON строка с конфигурацией

curl -X POST /api/certificates/import/analyze \
  -H "Authorization: Bearer <token>" \
  -F "file=@certificates.xlsx" \
  -F 'config={"courseSource":"manual","courseName":"Курс повышения квалификации","courseCode":"ATC25","validityType":"months","validityMonths":12,"urlTemplate":"http://edu.uzairports.com/certificates/{NUM}_{FIO}.pdf","createStudents":true,"updateExisting":false,"skipErrors":true}'
```

### Шаг 2: Выполнение импорта

```bash
# POST /api/certificates/import/execute
# Content-Type: application/json

# Используйте данные из ответа analyze
curl -X POST /api/certificates/import/execute \
  -H "Authorization: Bearer <token>" \
  -H "Content-Type: application/json" \
  -d '{"rows": [...], "config": {...}, "courseInfo": {...}}'
```

### Шаг 3: Отслеживание прогресса

```bash
# GET /api/certificates/import/status/{jobId}

curl -X GET /api/certificates/import/status/cert_import_1234567890_abc123 \
  -H "Authorization: Bearer <token>"
```
