# План реализации интерактивного дашборда

## ✅ Выполнено

### 1. Расширение API `/api/admin/dashboard`
- ✅ Добавлен запрос для получения распределения студентов по организациям (топ-10)
- ✅ Добавлен запрос для получения сертификатов по месяцам (за последние 12 месяцев)
- ✅ Добавлен запрос для получения топ-10 инструкторов по проведённым часам
- ✅ Добавлен запрос для получения топ-10 курсов по количеству групп
- ✅ Добавлен запрос для получения топ-10 курсов по количеству слушателей

### 2. Создание компонентов чартов
- ✅ Создан `DonutChart.vue` — универсальная круговая диаграмма с поддержкой:
  - Кастомных цветов
  - Легенды
  - Событий клика
  - Отображения общей суммы в центре
  - ClientOnly для SSR
  
- ✅ Создан `DynamicBarChart.vue` — универсальный bar chart с поддержкой:
  - Динамических данных
  - Горизонтальной/вертикальной ориентации
  - Кастомных цветов
  - Событий клика
  - ClientOnly для SSR

### 3. Обновление Admin Dashboard
- ✅ Добавлена круговая диаграмма "Студенты по организациям"
- ✅ Добавлен bar chart "Сертификаты по месяцам"
- ✅ Добавлена таблица "Топ инструкторов по часам" с:
  - Ранжированием (золото/серебро/бронза)
  - Аватарами с инициалами
  - Количеством часов и занятий
  
- ✅ Добавлен блок "Топ курсов" с переключением между:
  - Топ по количеству групп
  - Топ по количеству слушателей
  
- ✅ Добавлена интерактивность:
  - Hover-эффекты на всех карточках
  - Клики на карточки статистики ведут на соответствующие страницы
  - Анимации при наведении
  - Плавные переходы

## 📊 Реализованные компоненты

### Круговая диаграмма (DonutChart)
```vue
<ChartsDonutChart 
  chart-id="students-by-org"
  :series="[120, 85, 67, 45, 32]"
  :labels="['Организация 1', 'Организация 2', ...]"
  :height="350"
  legend-position="bottom"
  @click="handleClick"
/>
```

### Bar Chart (DynamicBarChart)
```vue
<ChartsDynamicBarChart 
  chart-id="certificates-by-month"
  :series="[{ name: 'Сертификаты', data: [12, 15, 8, ...] }]"
  :categories="['Янв', 'Фев', 'Мар', ...]"
  :colors="['#10B981']"
  :height="320"
  y-axis-title="Количество"
/>
```

## 🎨 Дизайн-решения

1. **Цветовая схема**:
   - Золото (#F59E0B) для 1-го места
   - Серебро (#E5E7EB) для 2-го места
   - Бронза (#F97316) для 3-го места
   - Градиенты для визуального выделения

2. **Интерактивность**:
   - Все карточки статистики кликабельны
   - Hover-эффекты с увеличением (scale-105)
   - Плавные переходы (transition-all)
   - Анимированные индикаторы загрузки

3. **Адаптивность**:
   - Grid-система для разных размеров экрана
   - Минимальная ширина для чартов
   - Горизонтальная прокрутка на мобильных

## 📈 Данные API

### Новые поля в ответе `/api/admin/dashboard`:

```typescript
{
  // Существующие поля...
  
  // Новые данные для чартов
  studentsByOrganization: [
    { name: string, count: number }
  ],
  
  certificatesByMonth: [
    { month: string, count: number } // month: "YYYY-MM"
  ],
  
  topInstructors: [
    { 
      id: string, 
      name: string, 
      hours: number, 
      lessonsCount: number 
    }
  ],
  
  topCoursesByGroups: [
    { 
      id: string, 
      name: string, 
      code: string, 
      groups_count: number 
    }
  ],
  
  topCoursesByStudents: [
    { 
      id: string, 
      name: string, 
      code: string, 
      students_count: number 
    }
  ]
}
```

## 🔧 Технические детали

### SSR Compatibility
- Все компоненты с ApexCharts обёрнуты в `<ClientOnly>`
- Добавлены fallback-индикаторы загрузки
- Проверка `mounted` перед рендерингом чартов

### Производительность
- Ограничение топ-списков до 10 элементов
- Оптимизированные SQL-запросы с индексами
- Ленивая загрузка чартов

### Обработка ошибок
- Try-catch блоки для всех SQL-запросов
- Graceful degradation при отсутствии данных
- Пустые состояния с иконками и сообщениями

## 🚀 Следующие шаги (опционально)

1. **Фильтрация по периодам**:
   - Добавить селектор периода для чартов
   - Реализовать динамическую загрузку данных

2. **Экспорт данных**:
   - Кнопки экспорта в CSV/Excel
   - Печать отчётов

3. **Дополнительные метрики**:
   - Средняя посещаемость
   - Процент успешно завершённых курсов
   - Динамика роста студентов

4. **Real-time обновления**:
   - WebSocket для live-данных
   - Автоматическое обновление каждые N минут

## 📝 Примечания

- Все компоненты следуют существующей дизайн-системе проекта
- Используются существующие цветовые токены (primary, success, warning, info)
- Соблюдены правила минимизации компонентов
- Все тексты на русском языке
- Добавлена валидация данных перед отображением
